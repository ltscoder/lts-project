回溯：
回溯+dfs(递归，非递归B12NQueen.dfsWithNoRecursion)


关于排列和组合：
1排列中使用boolean[] used = new boolean[nums.length]，解决的问题是 用过的元素不能再用
2排序+(nums[i] == nums[i - 1] && used[i - 1] == 0)判断,解决的问题是 nums相同值的元素导致的重复的排列。
3排序+for循环从上一个index+1开始，解决的问题是 nums没有相同值的元素时重复的组合。
  ~当nums有相同值时，相同值的元素也被当成不同元素了，3这种方式只能排除掉一部分重复的排列，有一部分排除不了，所以
   【给定一个有重复元素的int数组，和k，返回所有可能的 k 个数的组合。】要方式2和3结合使用。~具体参考B3combine.dfsCombine2方法。
~排列是有序的，组合是无序的；